const model = require('../Models/model.js');

const chatController = {};

chatController.getMessages = (req, res, next) => {
  console.log('in get messages', req);
  // const room = 
  // const queryText = 'SELECT '
  return next();
}

module.exports = chatController;